version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    <<: *defaults
    steps:
      - run:
          name: npm building
          command: |
            npm install
            CI=true npm run build
  test:
    docker:
      - image: alpine:3.7
    steps:
      - run:
          name: npm testing
          command: |
            npm install
            npm run test
  hithere:
    docker:
      - image: alpine:3.7
    steps:
      - run:
          name: useless
          command: |
            echo "heloooo"
workflows:
    build_test_and_lint:
      jobs:
        - build
        - hithere
        - test:
            requires:
              - hithere
