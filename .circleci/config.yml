version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - run: npm install
      - run: CI=true npm run build
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - run: npm install
      - run: npm run test
  hithere:
    docker:
      - image: circleci/node:latest
    steps:
      - run: echo "heloooo"
workflows:
    build_test_and_lint:
      jobs:
        - build
        - hithere
        - test:
            requires:
              - hithere
